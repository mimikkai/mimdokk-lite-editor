# Tasks

## Example Template
- [ ] Create directory `public/examples/contact-info`.
- [ ] Create/Place `example.docx` in `public/examples/contact-info/`.
- [ ] Add "Load Example" button to `TemplateUploader` or main `App` UI.
- [ ] Implement logic to fetch `example.docx`, convert to buffer, and trigger the parsing flow.

## User Guide
- [ ] Create `UserGuide` component.
- [ ] Document `{VAR}` syntax.
- [ ] Document `{VAR|default}` syntax.
- [ ] Add `UserGuide` to the main layout (e.g., as a collapsible section or modal).
